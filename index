<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>InnovativeElectronics — Get a Quote</title>
<style>
  :root{
    --bg:#0b0b0b;
    --card:#0f0f10;
    --gold:#d4af37;
    --muted:#cfcfcf;
    --accent:#ffd97a;
    --radius:14px;
    --maxw:920px;
  }
  *{box-sizing:border-box}
  body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial;background:linear-gradient(180deg,#050505 0%, #0b0b0b 100%);color:var(--muted);-webkit-font-smoothing:antialiased}
  .wrap{max-width:var(--maxw);margin:28px auto;padding:18px}
  header{display:flex;align-items:center;gap:14px}
  .logo{
    background:linear-gradient(180deg,var(--gold),#b98e2d);
    width:64px;height:64px;border-radius:12px;display:flex;align-items:center;justify-content:center;color:#070707;font-weight:700;font-size:20px;box-shadow:0 6px 18px rgba(0,0,0,.6)
  }
  h1{margin:0;font-size:22px;color:white}
  p.lead{margin:6px 0 0;color:#cfcfcf;opacity:.92}
  main{display:grid;grid-template-columns:1fr 420px;gap:28px;margin-top:18px}
  .hero{padding:20px;border-radius:14px;background:linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));box-shadow:0 8px 22px rgba(0,0,0,.6)}
  .hero .benefits{margin-top:14px;display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
  .chip{background:rgba(255,255,255,0.02);padding:10px;border-radius:10px;border:1px solid rgba(255,255,255,.03)}
  .form-card{background:var(--card);padding:18px;border-radius:14px;box-shadow:0 10px 30px rgba(0,0,0,.6);position:sticky;top:22px}
  label{display:block;margin:10px 0 6px;font-size:13px;color:#ddd}
  input[type=text], input[type=tel], input[type=email], select, textarea{
    width:100%;padding:10px;border-radius:8px;border:1px solid rgba(255,255,255,.06);background:transparent;color:var(--muted);
  }
  .row{display:flex;gap:10px}
  .row > *{flex:1}
  .btn{
    display:inline-block;padding:12px 16px;border-radius:10px;background:linear-gradient(90deg,var(--gold),#c79b3a);color:#050505;font-weight:700;border:none;cursor:pointer;margin-top:12px;width:100%;
    box-shadow:0 8px 20px rgba(212,175,55,.12);
  }
  .muted{color:#b8b8b8;font-size:13px}
  footer{margin-top:18px;color:#9b9b9b;font-size:13px}
  .small{font-size:12px;color:#bdbdbd}
  .status{margin-top:12px;padding:12px;border-radius:10px;display:none;font-weight:700;}
  .status.success{background:linear-gradient(90deg,#0a662a, #0b8b46);color:white;}
  .status.error{background:#8b1e1e;color:white;}
  @media(max-width:880px){
    main{grid-template-columns:1fr}
    .form-card{position:static}
  }
</style>
</head>
<body>
<div class="wrap">
  <header>
    <div class="logo">IE</div>
    <div>
      <h1>InnovativeElectronics</h1>
      <p class="lead">We buy phones for cash — get a quote in minutes.</p>
    </div>
  </header>

  <main>
    <section class="hero">
      <h2 style="color:white;margin:0 0 8px">Get a fast, fair offer for your phone</h2>
      <p class="muted">Fill the form with the phone details — we’ll text & email you a quote immediately and call if needed.</p>

      <div class="benefits">
        <div class="chip"><strong>Instant quote</strong><div class="small">No wait — get an initial offer now</div></div>
        <div class="chip"><strong>Top dollar paid</strong><div class="small">We price fairly, based on condition & IMEI</div></div>
        <div class="chip"><strong>Fast pickup</strong><div class="small">Local pickup or drop-off options</div></div>
        <div class="chip"><strong>Secure</strong><div class="small">We wipe devices before resale</div></div>
      </div>

      <h3 style="color:white;margin-top:18px">How it works</h3>
      <ol style="margin:8px 0 0;padding-left:20px;color:#bfbfbf">
        <li>Enter device details and IMEI</li>
        <li>Receive same day email or SMS with a quote</li>
        <li>Accept the offer & schedule pickup</li>
      </ol>
    </section>

    <aside class="form-card">
      <form id="quoteForm">
        <label>Full name <span class="small">*</span></label>
        <input type="text" name="name" required placeholder="John Doe" />

        <div class="row">
          <div>
            <label>Phone number <span class="small">*</span></label>
            <input type="tel" name="phone" required placeholder="(951) 376-4778" />
          </div>
          <div>
            <label>Email (optional)</label>
            <input type="email" name="email" placeholder="innovativeforeigners@gmail.com" />
          </div>
        </div>

        <label>Phone Brand & Model</label>
        <input type="text" name="model" placeholder="iPhone 12 Pro Max" />

        <div class="row">
          <div>
            <label>Storage</label>
            <select name="storage">
              <option value="">Select</option>
              <option>64 GB</option>
              <option>128 GB</option>
              <option>256 GB</option>
              <option>512 GB</option>
              <option>1 TB</option>
            </select>
          </div>
          <div>
            <label>Carrier</label>
            <select name="carrier">
              <option value="">Select</option>
              <option>Unlocked</option>
              <option>Verizon</option>
              <option>AT&T</option>
              <option>T-Mobile</option>
              <option>Metro</option>
              <option>Other</option>
            </select>
          </div>
        </div>

        <div class="row">
          <div>
            <label>Unlocked?</label>
            <select name="unlocked">
              <option value="">Select</option>
              <option>Yes</option>
              <option>No</option>
              <option>Not sure</option>
            </select>
          </div>
          <div>
            <label>Powers on?</label>
            <select name="powers_on">
              <option value="">Select</option>
              <option>Yes</option>
              <option>No</option>
              <option>Only charges</option>
            </select>
          </div>
        </div>

        <label>Condition</label>
        <select name="condition">
          <option value="">Select</option>
          <option>Like New</option>
          <option>Lightly Used</option>
          <option>Cracked Screen</option>
          <option>Back Cracked</option>
          <option>Water Damage</option>
          <option>Heavy Damage</option>
        </select>

        <label>Accessories included</label>
        <input type="text" name="accessories" placeholder="Charger, box, headphones, none" />

        <label>IMEI / Serial Number <span class="small">*</span></label>
        <input type="text" name="imei" required placeholder="15-digit IMEI or serial" />

        <label>Preferred contact</label>
        <select name="preferred_contact">
          <option>Text</option>
          <option>Call</option>
          <option>Email</option>
        </select>

        <label>Additional notes</label>
        <textarea name="notes" rows="3" placeholder="Repairs, buyback reason, etc."></textarea>

        <div style="margin-top:8px" class="small">By submitting, you agree we may contact you by phone or email about this request.</div>

        <button type="submit" class="btn">Get My Quote</button>
        <div id="status" class="status"></div>
      </form>

      <div style="margin-top:14px;font-size:13px;color:#bdbdbd">
        <strong>Need help?</strong>
        <div class="small">Call or text: <span id="yourPhone">1-951-376-4778</span></div>
      </div>
    </aside>
  </main>

  <footer>
    <div class="small">InnovativeElectronics • Secure offers • We pay cash for electronics!</div>
  </footer>
</div>

<script>
  const form = document.getElementById('quoteForm');
  const status = document.getElementById('status');

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    status.style.display = 'none';
    status.textContent = '';

    const formData = new FormData(form);

    if(!formData.get('name') || !formData.get('phone') || !formData.get('imei')){
      status.style.display = 'block';
      status.className = 'status error';
      status.textContent = 'Please fill in your name, phone number, and IMEI.';
      return;
    }

    const data = {
      name: formData.get('name'),
      phone: formData.get('phone'),
      email: formData.get('email'),
      model: formData.get('model'),
      storage: formData.get('storage'),
      carrier: formData.get('carrier'),
      unlocked: formData.get('unlocked'),
      powers_on: formData.get('powers_on'),
      condition: formData.get('condition'),
      accessories: formData.get('accessories'),
      imei: formData.get('imei'),
      preferred_contact: formData.get('preferred_contact'),
      notes: formData.get('notes')
    };

    try {
      const response = await fetch('https://formsubmit.co/ajax/innovativeforeigners@gmail.com', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify(data)
      });
      const result = await response.json();
      status.style.display = 'block';
      if(response.ok){
        status.className = 'status success';
        status.textContent = '✅ Quote request sent! We will contact you shortly.';
        form.reset();
      } else {
        status.className = 'status error';
        status.textContent = result.message || '❌ There was an issue sending your quote. Please try again.';
      }
    } catch(err){
      status.style.display = 'block';
      status.className = 'status error';
      status.textContent = '⚠️ Network error. Please try again later.';
      console.error(err);
    }
  });
</script>
</body>
